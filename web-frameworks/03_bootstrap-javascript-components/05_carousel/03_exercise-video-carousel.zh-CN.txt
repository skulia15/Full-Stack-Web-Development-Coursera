轮播是一个非常有趣的 Bootstrap 组件 它能将一系列图像在屏幕上
滚动着显示 事实上 使用轮播是一个相当方便的 为你的网站加入动态效果的方式 当然有时你可能会觉得它太花哨了 不过不管怎么说 接下来让我们
学习轮播组件的相关知识吧 首先你要知道 轮播组件是一个
基于 JavaScript 的组件 所以为使其正确运作
我们要引入相应的 JavaScript 支持 接下来我们要在 index.html 的抬头
或说在其超大屏幕之下 加入一个轮播组件 让我们来到 index.html 中 我将会在这个超大屏幕下面
引入一个轮播 我将会把主体内容中剩下三列的内容 移到轮播中 事实上我有打算直接把代码拷过去 轮播将会出现在这超大屏幕之下 当我们实现完轮播后
我们会回来看看其效果 然后你可以看看你是更喜欢轮播 还是更喜欢现在这个样式 不过不论如何 轮播都是一个
有趣的组件 所以让我们来学习其实现吧 来到 HTML 代码这里
我将会实现另一个 content row 也就是一个设置了
class="row row-content" 的 div 然后我会把轮播放在这个 div 中 你可以看到我把代码贴了上来 然后对第二层 div, 我对其设置了
class="col-xs-12" 以占满所有的 12 列 然后这第二层 div 之内
就是我实现轮播的地方 让我先把代码贴上来 然后我们再来看它是怎么运作的 首先是一个设置了 class="carousel slide" 的 div 我还把其 id 设置为了 mycarousel 以及设置了 data-ride="carousel" 然后在这个 div 中 我加进了另一个 div 对这个 div 我设置了
class="carousel-inner" 然后这个 div 中将会包含所有 轮播将要展示的内容 在这个 div 里面 我们要把每一项要展示的内容
封装进一个设置了 class="item" 的 div 中 然后对其中一个 div 还要追加设置
class="active" 这个设置表明当轮播刚开始运作时
该 div 对应的内容将会第一个被呈现 我们可以在我们要展示的内容中
通过 img 标签加进一个图像 你可以看到我已经把我们之前
引入过的那些菜的图片所对应的代码 都贴到了这里 然后除了图像之外 我还引入了对图像的说明 所以我在这里放了一个设置了 class="carousel-caption" 的 div 我可以在这个 div 里面放
相应的菜的信息 事实上你也可以看到 我把相应的菜的信息的代码
都贴了过来 首先我把第一行的菜对应的信息贴了过来 然后是第二行的 然后是第三行的 对每一行的菜 要注意把它的
图像对应的代码和信息对应的代码分开 图像对应的代码可以直接贴过来
信息对应的代码则需封装在一个 div 中 当我们把这三道菜对应的代码
都贴好了之后 我们还需要对我们的 CSS 文件
做一些小小的更改 从而使我们的轮播能够
正确地在屏幕上显示 进入到我们的 mystyles.css 文件 我已经引入了一些 CSS 类 让我们快速地看看它们 并了解它们有啥用 第一个引入 CSS 类是 carousel 类 我设置了 background: #1A237E
意味着我将其背景色设为了深靛蓝色 这是因为这个颜色
正是我们导航栏的颜色 从而我们的页面看上去色调是一致的 事实上在设计页面时
保持色调的一致性相当重要 然后对 .carousel .item 组件 我将其高度设置为了 300 像素 由此 我们轮播的高度就会被固定下来 接下来是 .item img 类
这是拿来定制轮播中的图像的 我将其定位设置为了绝对定位
以及将左上角的坐标设置为了 0, 0 这意味着轮播中的图像会
贴在轮播的左边缘 同时左上角坐标为 0, 0 从而图像会贴在 轮播的左上角 我还将图像的最小高度
设置为了 300像素 从而无论图像本身大小如何 该轮播都会将图像的高度
至少拉伸到 300 像素 如果你引入了一个更大的图像的话
那么就会是轮播自身 进行合适的拉伸以包含该图像 那么做完这些改动后
让我们回到我们的网页并看看效果吧 回到我们的网页来 你可以看到这里有一个轮播 如果我将鼠标悬停在轮播上
那么这个轮播就不会继续滚动 如果我将鼠标移开 该轮播就会继续以一定时间间隔
来继续滚动 默认的时间间隔为三秒 也就是说每隔三秒 轮播中的内容
就会更新一次 那么这就是轮播的效果 接下来我将实现两个
对该轮播的控制 第一个控制是在轮播底部显示三个点 每个点对应着一张轮播中的幻灯片 通过点击对应的点
就可以让轮播跳到对应的幻灯片 第二个控制是在轮播的左右
各引入一个按钮 当我们点击它们时 轮播就会向左或向右滚动一下 那么 先让我实现那三个点
或说是轮播的指示器吧 那么在轮播里面 我放了一个 ol 标签
同时 我对其设置了 class="carousel-indicators" 然后在 ol 中 我放了三个 li 标签
对它们的每一个 我都设置了 data-target="#mycarousel"
和 data-slide-to 属性 这个属性是拿来指定其导向的
幻灯片的下标的 如果你想让该指示器
将你导向第一张幻灯片 由于下标是从 0 开始的
所以你只需设置 data-slide-to="0" 即可 同时 对相应的 li
设置上 class="active" 接下来就是一左一右两个按钮了 当我们点击这两个按钮时 该轮播会相应地 向左或向右滚动一下 以左边的按钮为例 实现的方法是应用 a 标签
并对其设置上 role="button" 以表示其为按钮 同时还要设置
class="left carousel-control" 和 href="#mycarousel" 来指向轮播 然后在 a 标签里面 我应用了一个
相应的 glyphicon 图标 同理设置好右边的按钮 只要把所有 left 改成 right 就差不多了 做完这些后 让我们回到我们的网页 并看看有哪些变化 来到我们的网页 你可以看到轮播下面有三个点 其中一个是填充了的点
另外两个则是空心的 这三个点正是该轮播的指示器 比如如果我想看第三张幻灯片 我只需点击这第三个点即可 这些指示器事实上 就是这个轮播的一个微型导航 同时 你可以看到轮播的左右边缘处 分别有一个向左和向右的小箭头 当我点击向右的箭头时
轮播会滚向下一张幻灯片 同样的 如果我点击向左的这个箭头
轮播就会向前滚动一下 总之这些功能 能够让你人工地控制该轮播的表现 所以说轮播是一个有趣的组件
它为你的网页 加入了一些动态效果
【翻译 - Carefree0910】