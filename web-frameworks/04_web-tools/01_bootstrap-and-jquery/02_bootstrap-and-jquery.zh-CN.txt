现在 让我向你介绍一下应该如何同时使用
Bootstrap 和 jQuery 来实现 Bootstrap 对 JavaScript 的一些支持功能 Bootstrap 对 JavaScript 的支持
是通过 JS 插件来实现的 而这些插件使用 jQuery 写的 我们已经学过如何单独地将某些插件
应用到我们的网页中 又或将整个插件组一起引用 我们也已经通过这张图学习了 
JavaScript, jQuery 和 Bootstrap 中的 JS 组件 之间的关系 我们还学习了如何在
一行 JavaScript 代码也不写的情况下 通过数据属性来使用 Bootstrap 的 JS 组件 但显而易见的是 如果我们需要使用
完整的 JavaScript 接口 我们就需要写一些代码了 而这也就是我们在这一节需要做的事 探索如何用 jQuery 或 JavaScript 代码
来控制 Bootstrap 组件 让我先向你一个 jQuery 的概要吧 jQuery 是一个轻量级的 JavaScript 库 不过它的功能非常丰富 同时也提供了很多 操控 HTML 或 DOM 的方法 从而你可以用它来直接控制 HTML 元素 它还允许你增加或移除 CSS 类或是操控 CSS 还允许你对 HTML 事件作出响应 并根据不同的的事件来执行特定的代码 它甚至允许你实现各种炫酷的效果和动画 同时还允许你通过 AJAX 去和后端服务器交互 话说 Bootstrap 的 JavaScript 组件 就是用 jQuery 搭建的 现在让我们简单地学习一下 jQuery 的语法 从而可以知晓如何通过它控制
Bootstrap 的 JavaScript 组件 首先 我们通常会用 $ 作为 jQuery 语句的开头 因为 $ 会告诉电脑
我们接下来准备要用 jQuery 了 在 $ 之后我们会有一个括号
括号里面的东西叫选择器 选择器代表的是一个特定的 HTML 元素 有许多方法可以用来指定选择器 我们马上就会学到 在选择器后 我们将会指定一个作用在所选择的
HTML 元素上的动作 这个动作需要和所选择的元素相匹配 如图所示便是一个栗子 我选择了 p 这个 HTML 元素
并指定了 hide 这个动作 这意味着当这段代码执行时 所有 p 打头的内容都会被隐藏 类似的 我们还可以用 id 来选择元素 比如这里 我们就选择了
id 为 mycarousel 的轮播元素 然后我们指定了 carousel 'pause' 动作 意味着这将会使指定轮播的滚动停止 现在让我们来看一个完整的例子 如图所示 这些代码被框进了 script 标签中 在这段代码中 我们可以看到我们指定了
document 元素和 ready 动作 意味着当整个 Bootstrap 文档准备好了后
ready 中的函数将会执行 而函数是定义在括号里的 这个函数实现了如下功能 让所有 data-toggle 属性为 tooltip 的元素 执行 tooltip 动作 注意 中括号中的语句其实可以看做是筛选器 所以这就是如何让所有含有 tooltip 属性的元素 都支持工具提示的方法 你是可以将 任一个 HTML 元素指定为 jQuery 选择器的 所以任一个 HTML 标签 比如 p 或 button 都可以指定为选择器 这意味着所有相应标签的 HTML 元素 都会被选为选择器 你也可以通过 HTML 元素的 id 来单独地指定一个元素 你也可以指定一系列拥有 一个特定类或 CSS 类的元素 所以你可以指定所有 btn 类元素或是
所有 btn-default 类元素 你还可以同时指定多种类 这样的话 就只有那些
同时拥有这些类的元素会被选择 你也可以通过属性来指定 比如说 你可以只选择那些 属性里有 href 的元素 又或像之前说过的那样 只选择那些 data-toggle 属性
被设置为 tooltip 的元素 当前的元素可以用 this 来指定 使用 this 就意味着当前元素被选择器选择了 还有其它很多选择器的相关方法
这里就不一一赘述了 很多情况下 用户与网页的交互 都会激发一个 DOM 事件 而 jQuery 提供了方法
去响应这些 DOM 事件 DOM 事件包括但不限于 鼠标单击 鼠标进入 鼠标移出和键盘交互 又或是和表格的提交 以及往一个输入框输入信息 选择中或取消选择一个输入框 或甚至 对整个 document 窗口的操作 比如载入 重载大小或卸下窗口 所有的这些 DOM 事件都可以用 jQuery 方法
去响应 让我们用 Bootstrap 中的轮播作为例子
来更好地理解 如何运用 JavaScript 方法和 jQuery 方法 正如我们学过的 Bootstrap 的 JavaScript 组件可以通过设置数据属性来使用 而对于轮播 如图所示 我们可以设置四种数据属性 而若想用 JavaScript 来控制这轮播 我们就可以这样指定 选择器通过指定 carousel 类 来选出拥有 carousel 类的元素
然后对这些元素作出 carousel 动作 carousel 动作可以囊括之前用数据属性
设置的所有东西 比如说设置间隔 我们可以将轮播的滚动间隔调至一个特定的值 在这个例子里面 我们就将间隔设置为了 2000 毫秒 也就是 2 秒 我们也可以设置其它属性 比如 cycle 这是让你设定你的轮播是向左滚还是向右滚 比如 pause 可以让你设定是否将滚动暂停 比如 number 可以让你设置滚动到哪一页 你可以设置为 0 或 1 或 2
来表示你要滚动到第 0 1 2 页 你可以用 prev 或 next 来设置 是否去往当前页面的前一页或后一页 需要注意的是 轮播本身在出现的时候 会激发两个事件 其一是 slide.bs.carousel 事件 这会在滚动动画开始时被激发 而 slid.bs.carousel 事件则会在
轮播完成了向下一页面过渡的动画后 被激发 那么我们该如何在 jQuery 方法中
利用这两个事件呢 如图所示 你可以先用 id 来指定你的轮播 然后你需要在之后指定一个 on 动作 然后 on 动作接受的第一个参数需是
上述说的两个事件之一 就是你想要响应的那个事件 在这个例子里 我们想要响应
slide.bs.carousel 于是在函数体内 我们就可以指定我们想要在事件被激发后
响应的代码了 在我们下个练习中 我们将会看到
以上这些东西的具体实现 我们还会在轮播中加入两个按钮 这两个按钮允许我们暂停和继续轮播的滚动 当然 我们将会学习如何用 JavaScript 代码去实现
【翻译 - Carefree0910】