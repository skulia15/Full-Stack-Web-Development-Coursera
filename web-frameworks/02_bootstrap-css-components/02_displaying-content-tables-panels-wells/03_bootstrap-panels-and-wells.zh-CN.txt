接下来我们将学习两个 Bootstrap 组件 这两个组件都是拿来显示内容的 它们分别是面板和 Well 有时你可能会想高亮表示
你网页中的某些内容 从而用户将会更有可能注意到它们 而面板和 well 正是两种你可以 用一个新页面来高亮显示某些内容的方法 面板 顾名思义 允许你在一个盒子里 显示一些内容 如图所示 你可以为这个盒子设置一个简单的边界 这也是一个标准的面板会做的事 你也可以为你的面板加一个标题 在这个栗子里 我们通过设置
class="panel-heading" 来指定一个写标题的 div 块 在这个 div 块中还需要一个设置了
class="panel-title" 的某个 h 标签 在这个栗子里我们的用的 h 标签是 h3 而在 h3 标签中的内容自然就是我们想要 出现在面板标题中的内容 而在 panel-body 的下面则可以包含
任一个 HTML 元素 在这个栗子里 我用的是一个描述表 通过这个描述表我可以显示餐厅的一些信息 而事实上显示信息确实是描述表的用法之一 注意到 我这里对描述表设置了
class="dl-horizontal" 所以这些信息都分别整齐地排列在了 同一水平线上 对于面板 你还可以定义页脚 如果你确实需要用到页脚的话 你完全可以像定义标题那样 去定义它 接下来要学习的 Bootstrap 组件是 Well 正如这个栗子所示 一个 well 就像是一个 有着淡灰色背景的小盒子 在 well 里面你可以加入任何内容 使用 well 的方法也很简单 只需要设置一个
div 的类为 well 即可 然后在这个 div 的下面 你可以封装进
任何 HTML 元素 这些元素将会在一个淡灰色背景的盒子中呈现
如图右侧所示 在这个栗子里 我还用到了 blockquote 来 显示我对用户言论的引用 当你应用 blockquote 时 你的内容将会
像图中那样被格式化 同时一个垂直的线会出现在 整个 blockquote 的左侧 我们同样可以对 blockquote 设置页脚 通常我们会在页脚处表明所引用语句的出处 well 的大小也是可以设置的 只需要将 well-lg 等等设置进类就好 图中所示的 well 是一个默认大小的 well 如果你想要一个大点的 well 只需设置 class="well well-lg" 就行了 同理如果想要小一点的 well 的话就用 well-sm 接下来 我们将会进行下一个练习 在这个练习里 我们将会在我们的页面中
实现表格 面板和 well 我们将会定制一个 Bootstrap 表格 并呈现一些表格数据 定制的方法自然是通过各种各样的
Bootstrap 表格类 最后 我们将引入一个面板和一个 well
【翻译 - Carefree0910】